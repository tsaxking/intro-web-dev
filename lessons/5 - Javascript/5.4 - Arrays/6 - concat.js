// Concat combines two arrays
const arr1 = ["a", "b", "c"];
const arr2 = [1, 2, 3];
const arr3 = ["that's how easy", "love can be"];

const arr = arr1.concat(arr2, arr3);
console.log(arr); // [ 'a', 'b', 'c', 1, 2, 3, 'that's how easy', 'love can be' ]