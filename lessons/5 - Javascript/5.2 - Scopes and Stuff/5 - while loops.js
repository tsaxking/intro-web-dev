// While loops will repeat code while a condition is true
// For example:
let num = 0;
// This code will run while num is less than 5
while (num < 5) {
    console.log(num);

    // This is the same as doing num = num + 1 or num += 1.
    num ++;
}
